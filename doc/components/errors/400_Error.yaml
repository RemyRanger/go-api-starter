description: Bad Request
content:
  application/json:
    schema:
      $ref: 'ModelError.yaml'
    example:
      error: Bad Request
      status: 400
      details:
        - field: GatewayMode
          msg: "Field validation failed on the 'oneof' tag"

